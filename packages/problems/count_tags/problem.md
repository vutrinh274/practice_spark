Given a table `products_with_arrays` with columns `product_id`, `product_name`, and `tags` (a comma-separated string of tags), count **how many tags each product has**.

Return columns: `product_id`, `product_name`, `tag_count`

Order by `tag_count` descending, then `product_id` ascending.
