services:
  spark-connect:
    image: apache/spark:4.0.0
    container_name: spark-connect
    hostname: spark-connect
    ports:
      - "15002:15002"
    volumes:
      - ./packages/problems:/problems:rw
    environment:
      - SPARK_NO_DAEMONIZE=true
    command: >
      /opt/spark/sbin/start-connect-server.sh
      --packages "org.apache.spark:spark-connect_2.13:4.0.0"
      --conf "spark.driver.extraJavaOptions=-Divy.cache.dir=/tmp -Divy.home=/tmp"
      --master "local[*]"
      --conf spark.scheduler.mode=FAIR
      --conf spark.connect.session.ttl=1h
    healthcheck:
      disable: true

  # nginx and api run locally via: make api / make web
  # For full Docker stack use: make docker-full (docker-compose.prod.yml)
